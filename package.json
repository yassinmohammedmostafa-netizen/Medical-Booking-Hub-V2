{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --if-present run build && node scripts/post-build.mjs",
    "start": "pnpm --filter @workspace/api-server run start"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
